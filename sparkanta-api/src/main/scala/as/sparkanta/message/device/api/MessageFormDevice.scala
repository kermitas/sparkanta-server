package as.sparkanta.message.device.api

/**
 * Marker trait. It defined that this message comes from device and can be deserialized (from byte protocol to normal object).
 */
trait MessageFormDevice
